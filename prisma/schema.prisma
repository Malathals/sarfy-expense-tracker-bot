generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Expense {
  id        Int      @id @default(autoincrement())
  item      String
  amount    Float
  category  String?
  note      String?
  provider  String?
  createdAt DateTime @default(now())
}